# ETH 四形态 R2/R3 代码接入与仓位说明

## 1. 交付版本与定位

| 版本 | 四形态组合 | 当前定位 | 类名 |
|---|---|---|---|
| R2 | F1固定 + F2 MID + 旧严格F3 + F4平衡 | 冻结对照锚点；仍无实盘许可 | `EthSpikeR2Strategy` |
| R3 | F1跌破信号低点 + F2 ANY + F3阳线吞没 + F4平衡 | 研究/影子版本；不是已批准的R2升级 | `EthSpikeR3Strategy` |

不要把文件直接改名覆盖R16后就启动。原R16只返回收盘后的即时方向信号；R2/R3包含延迟确认和挂单状态，执行层必须接入本文第4节的两个 intent 接口。

## 2. 四种形态与开仓方向

| 形态 | 前置背景 | 插针方向 | 开仓 | R2确认 | R3确认 |
|---|---|---|---|---|---|
| F1 | 上涨 | 上插针 | 空 | 3根内跌破实体下沿减0.10 ATR | 3根内跌破信号K最低点，无缓冲 |
| F2 | 上涨 | 下插针 | 空 | 2根内跌破信号K最低点；收盘位置MID | 2根内跌破信号K最低点；关闭MID |
| F3 | 下跌 | 上插针 | 多 | 2根内收盘突破信号K实体上沿 | 4根内出现吞没信号K实体的阳线，阳线实体≥0.30 ATR |
| F4 | 下跌 | 下插针 | 多 | 3根内突破信号K实体上沿 | 与R2相同 |

共同规则：15分钟收盘生成候选；F1/F2/F4使用触发价确认；F3确认后只允许下一根15分钟开盘入场；同方向最多2次；相反方向信号不自动反手；TP 0.60%、SL 0.90%，成交或加仓后必须根据交易所返回的持仓均价重新挂 reduce-only 保护单。

主要形态阈值已固化在两个 Python 文件的 `frozen_shape_configs()` 中。Pine 把主要阈值做成输入，便于图表诊断，但改变输入后就不再是冻结的 R2/R3。

## 3. “3%仓位”在代码中的含义

默认环境变量按“保证金占权益3%”解释：

```text
账户权益 10,000 USDT
单次保证金 = 10,000 × 3% = 300 USDT
100倍下单名义金额 ≈ 300 × 100 = 30,000 USDT
单次实际名义敞口 ≈ 3倍账户权益
同向最多2次时，最大名义敞口 ≈ 6倍账户权益
```

这不是此前审计的 `C10_M2`。`C10_M2` 是每次名义仓位等于1倍权益、最多2次；在100倍杠杆下，大约只占1%保证金/次。默认3%保证金会把每次名义风险提高到 C10_M2 的3倍，历史收益和回撤百分比不能直接套用，也没有通过R2/R3审计。

如果你所说的“3%仓位”其实是“名义金额只占权益3%”，应改为：

```bash
FOURSHAPE_POSITION_MODE=NOTIONAL_PCT
FOURSHAPE_POSITION_VALUE=3
```

默认按你当前表达采用：

```bash
FOURSHAPE_POSITION_MODE=MARGIN_PCT
FOURSHAPE_POSITION_VALUE=3
FOURSHAPE_LEVERAGE=100
FOURSHAPE_MAX_ENTRIES=2
FOURSHAPE_MAX_TOTAL_NOTIONAL_MULTIPLE=6
FOURSHAPE_ENTRY_API=INTENTS
```

环境变量在策略对象初始化时读取，修改后需要重建策略对象或重启进程。

## 4. 执行层必须怎样接入

### 4.1 每根15分钟K线收盘

1. 将只含已完成K线、索引为K线开始时间的 DataFrame 传给策略。
2. 调用 `strategy.pending_entry_intents(df)`：返回本根K线新建的 F1/F2/F4 止损触发挂单意图。
3. 调用 `strategy.next_open_entry_intents(df)`：若本根完成了F3收盘确认，返回“下一根15分钟开盘”意图。
4. 对同时间、同方向信号按 F1→F4 顺序去重；不能自动反手。

### 4.2 每根1分钟K线或实时行情

- 对尚未到期的 F1/F2/F4 调用 `evaluate_pending_intent(...)`。
- 研究回放的保守规则是同一分钟同时碰到失效位和触发位时，先判失效。
- 实盘交易所会按真实逐笔先后成交；如果使用交易所原生 stop-entry，执行层仍要监控失效位并及时撤单。
- 到期、失效、距离过远或已有反向仓位时，必须撤销对应入场单。

### 4.3 F3下一开盘

- 在 `NextOpenEntryIntent.execute_at` 对应的真实开盘调用 `evaluate_next_open_intent(...)`。
- 该函数用真实开盘价与原始信号K收盘价计算最大距离；过远就不入场。
- 错过这一开盘后意图立即过期，不能稍后追价。

### 4.4 数量与保护单

执行层在实际发单前调用：

```python
size = calculate_order_size(
    equity=exchange_equity,
    order_price=planned_price,
    current_notional=current_same_strategy_notional,
)
```

成交后读取交易所实际加权持仓均价，再调用：

```python
levels = build_protective_levels(
    position_side=1,               # 多=1，空=-1
    average_entry_price=avg_price,
    price_tick=instrument_tick,
)
```

然后撤销旧保护单，重新提交 reduce-only TP/SL。策略文件本身不掌握账户权益，也不直接提交交易所订单；只替换 Python 信号文件不会自动把仓位改成3%。

## 5. 兼容模式

`FOURSHAPE_ENTRY_API=INTENTS` 是默认值，也是推荐方式。在该模式下，旧的 `generate_signal()` 会故意返回 `NO_SIGNAL`，防止执行层把延迟确认错误地当成即时市价单。

只有暂时无法改造执行器时，才可使用：

```bash
FOURSHAPE_ENTRY_API=CONFIRMED_BAR_COMPAT
```

兼容模式通过15分钟K线近似确认，不能完全复现1分钟内的触发/失效先后、跳空成交和F3下一开盘距离检查，不能作为正式逐笔审计口径。

## 6. Pine与Python的边界

两份 Pine 都是 TradingView 研究策略，默认使用3%保证金、100倍、最多2次，并可在设置中切换仓位语义。建议在15分钟图使用 Bar Magnifier。

Pine仍存在以下不可消除的差异：

- 同一低周期内同时触发入场与失效时，历史撮合顺序不一定与Python“失效优先”相同；
- F3在Python中按下一根真实开盘检查距离，Pine只能在确认收盘处近似过滤后，于下一根开盘撮合；
- 多个重叠候选的挂单状态在Pine中做了单形态合并，Python正式状态机应保留每个唯一意图；
- Pine手续费设为每边0.035%，只是对“入场0.05%+出场0.02%”的对称近似。

因此 Pine 用于看图和初筛，逐笔对账、审计及实盘信号以 Python intent 状态机为准。

## 7. 已完成验证

- 两个 Python 文件语法编译通过；
- R2/R3 的 F1、F2、F3、F4 原始候选与正式研究引擎逐行完全一致；
- R3 F3 阳线吞没开发期25个实际入场事件与研究证据逐笔完全一致；
- 3%保证金换算、6倍名义总上限、合约步长、TP/SL方向性取整通过；
- 同一分钟失效优先、F3真实下一开盘距离和错过即过期测试通过；
- Pine完成离线静态核对，但仍需粘贴到TradingView编辑器做平台在线编译。

## 8. 当前审计结论

R2是冻结对照，但不是获批实盘策略。R3历史结果有改善线索，但2026年8月样本外正常成本为负；F3阳线吞没只有少量样本且8月唯一一笔止损。因此更稳妥的替换顺序是：先用 R2 影子运行确认执行层逐笔一致，再并行记录 R3，不要直接让 R3 接管原 R16 的真实订单。
